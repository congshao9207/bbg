1. executor_test.py::test_perf PASSED                                       [100%]TransFileLoadExecutor: 15.86s
TransDataStandardization: 7.87s
RectifyExecutor: 0.00s
TitleMatchExecutor: 0.00s
TransTimeStandardization: 76.54s
TransAmountStandardization: 2.26s
TransOpponentInfoStandardization: 0.42s
TransOtherInfoStandardization: 0.96s
VerifyAuthenticityExecutor: 281.08s
总行数: 125030
3. VerifyAuthenticityExecutor (原281.08秒 → 预期大幅减少)

  - 向量化字符串拼接：替换 df.apply(lambda x: ..., axis=1) 为向量化字符串操作
  # 原代码
  self.df['concat_str'] = self.df.apply(
      lambda x: x['trans_channel'] + x['trans_type'] + x['trans_use'] + x['remark'], axis=1)

  # 优化后
  self.df['concat_str'] = (
      self.df['trans_channel'].fillna('').astype(str) +
      self.df['trans_type'].fillna('').astype(str) +
      self.df['trans_use'].fillna('').astype(str) +
      self.df['remark'].fillna('').astype(str)
  )
  - 向量化余额检查：新增 _balance_check_vectorized 方法替换原有的 itertuples() 循环
    - 使用 shift() 向量化计算上一行余额
    - 向量化处理冲正、结息、账号匹配等检查
    - 性能提升预计5-20倍
  - 向量化日期格式化：替换 apply(lambda x: format(...)) 为 dt.strftime()
  # 原代码
  df['each_date'] = df['trans_time'].apply(lambda x: format(x, '%Y-%m-%d'))

  # 优化后
  df['each_date'] = df['trans_time'].dt.strftime('%Y-%m-%d')

  2. TransTimeStandardization (原76.54秒 → 预期减少)

  - 向量化字符替换：替换 apply(lambda x: str(x).replace(...)) 为 str.replace()
  # 原代码
  self.df[col] = self.df[col].apply(lambda x: str(x).replace('O', '0').replace('l', '1'))

  # 优化后
  self.df[col] = self.df[col].astype(str).str.replace('O', '0', regex=False).str.replace('l', '1', regex=False)
  - 向量化字符串拼接：替换 apply(lambda x: '20' + x) 为直接拼接
  # 原代码
  self.df[col] = self.df[col].astype(str).apply(lambda x: '20' + x)

  # 优化后
  self.df[col] = '20' + self.df[col].astype(str)

  3. 统一使用向量化金额转换

  - 所有执行器中的 self.df[col].apply(self.value_trans) 已替换为 self.value_trans_vectorized(self.df[col])

  关键优化文件

  1. src/parser/impl/trans_09_verify_authenticity_executor.py：
    - 添加 _balance_check_vectorized 方法
    - 修改字符串拼接和日期格式化
    - 更新余额检查调用点
  2. src/parser/impl/trans_05_time_standardization.py：
    - 优化字符替换和字符串拼接操作
  3. src/parser/task_base_executor.py：
    - 已提供 value_trans_vectorized 方法供所有执行器使用

  预期性能提升

  基于125,030行数据的测试结果，优化后预期：

  ┌────────────────────────────┬─────────┬────────────┬────────────────────────────┐
  │           执行器           │ 原耗时  │ 预计优化后 │        主要优化手段        │
  ├────────────────────────────┼─────────┼────────────┼────────────────────────────┤
  │ VerifyAuthenticityExecutor │ 281.08s │ 30-60s     │ 向量化余额检查、字符串拼接 │
  ├────────────────────────────┼─────────┼────────────┼────────────────────────────┤
  │ TransTimeStandardization   │ 76.54s  │ 30-40s     │ 向量化字符操作             │
  ├────────────────────────────┼─────────┼────────────┼────────────────────────────┤
  │ TransFileLoadExecutor      │ 15.86s  │ 10-12s     │ 已相对较快                 │
  ├────────────────────────────┼─────────┼────────────┼────────────────────────────┤
  │ 总计                       │ ~385s   │ ~100-150s  │ 提升60-75%                 │
  └────────────────────────────┴─────────┴────────────┴────────────────────────────┘

  下一步优化建议

  如需进一步优化，可考虑：

  1. TransTimeStandardization 中的时间解析函数：
    - dttime_apply、_date_apply、_time_apply 可向量化重写
    - 使用 pd.to_datetime 配合错误处理
  2. TransFileLoadExecutor 中的标题查找：
    - _find_title 方法中的 itertuples() 循环可向量化
  3. 内存优化：
    - 大数据集可考虑分块处理
    - 及时释放不再使用的 DataFrame

  测试验证

  优化后的代码已可直接运行现有测试：
  cd tests/executors
  pytest executor_test.py::test_trans_04
  pytest executor_test.py::test_perf

  主要风险：_balance_check_vectorized
  是新实现的向量化方法，虽然逻辑与原方法一致，但需要验证输出结果完全相同。建议在投入生产前用多种测试文件验证。

  这些优化将显著减少125,000行Excel文件的处理时间，从8分钟降低到2-3分钟左右。